import type { GameMeta } from '../../types/game';

const meta: GameMeta = {
  slug: 'template',
  title: 'Template Game',
  description: 'Reference template for new games in the hub.',
  tags: ['Template'],
  ageRange: 'N/A',
};

export default meta;